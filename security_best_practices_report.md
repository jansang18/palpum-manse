# 취명선 전설의 만세력 보안 모범 사례 보고서

## 요약

이 앱은 별도 서버 계정이나 원격 데이터베이스 없이 동작하는 정적 PWA다. 사용자가 저장한 명반은 현재 기기의 브라우저 저장소에만 보관하며, `legend-saju:` 네임스페이스를 사용해 원본 앱의 데이터와 분리한다. 저장·가져오기·공유·궁합 경로에서 사용자 입력을 HTML로 실행하지 않도록 정규화와 DOM 안전 렌더링을 적용했다.

이번 릴리스 감사에서는 `360`, `390`, `412`, `768`, `1220px`와 200% 확대를 검사했다. 페이지 예외, 콘솔 오류, 실패한 요청, 가로 넘침은 모두 0건이었다. Android 프로젝트가 없는 독립 웹 저장소이므로 APK 전용 백업·서명 검사는 웹 기본 테스트에서 명시적으로 제외한다.

## 데이터 보호

### 로컬 전용 저장

- 명반은 `localStorage` 또는 호환 저장 어댑터의 `legend-saju:record:` 영역에만 저장한다.
- 테마와 폴백 설정 키는 명반 목록에서 제외한다.
- 원본 앱의 `saju:` 데이터와 서비스워커 캐시는 읽거나 삭제하지 않는다.
- 서비스워커는 `legend-manse-`로 시작하는 오래된 캐시만 정리한다.

브라우저 저장소는 암호화된 비밀 저장소가 아니다. 잠금 해제된 기기나 브라우저 프로필에 접근할 수 있는 사용자를 방어하지 못하므로, 매우 민감한 메모는 저장하지 않는 것이 안전하다.

### 가져오기 정규화

- 백업 파일은 2MB, 레코드는 500개로 제한한다.
- plain object와 허용된 필드만 받아들이고 알 수 없는 필드는 폐기한다.
- 날짜·시간·성별·즐겨찾기·저장시각의 타입과 범위를 검증한다.
- 이름은 40자, 메모는 240자로 제한한다.
- 외부에서 받은 파생 사주 값은 신뢰하지 않고 내부 계산기로 다시 만든다.
- 가져온 ID는 `crypto.randomUUID()` 또는 `crypto.getRandomValues()` 기반 UUID v4로 교체한다.
- `신의 음성 만세력`, `취명선 만세력`, `취명선 전설의 만세력` v1은 정확히 일치하는 제품명과 버전만 허용하고, 정상화된 레코드를 새 `legend-saju:` 네임스페이스에만 쓴다.
- 음력 레코드는 원 입력 날짜와 `isLeapMonth` 불리언을 요구하며 존재하지 않는 윤달을 평달로 대체하지 않는다.
- 일부 저장이 실패하면 시도한 레코드를 롤백하고, 남은 ID가 있으면 성공으로 보고하지 않는다.
- 주 저장소나 폴백 저장소의 읽기 상태가 불확실하면 빈 목록으로 위장하지 않고 오류를 표시한다.

## 실행 코드 방어

### 사용자 입력과 DOM

- 저장 카드와 궁합 이름은 `document.createElement`, `textContent`, `createTextNode`, `dataset`으로 구성한다.
- 이름과 메모를 `innerHTML` 또는 `insertAdjacentHTML`에 연결하지 않는다.
- 공유 이미지는 Canvas `fillText`로 그리며 사용자 문자열을 HTML로 해석하지 않는다.
- 백업 이름에 `<img onerror>` 또는 `<svg onload>`가 있어도 실행 가능한 노드가 생성되지 않는다.

### 외부 네트워크

- JSONP와 동적 제3자 `<script>` 실행 경로를 사용하지 않는다.
- 온라인 인물 보강은 HTTPS와 명시된 Wikipedia/Wikidata 호스트·경로만 허용한다.
- 요청은 `credentials: "omit"`, 리디렉션 거부, 타임아웃을 사용한다.
- 외부 요청이 실패하면 내장 데이터만 사용하는 방식으로 안전하게 축소한다.

## 공급망

`package.json`과 `package-lock.json`에 다음 버전을 정확히 고정한다.

- `manseryeok@2.0.0`
- `esbuild@0.25.8`
- `puppeteer-core@24.16.0`

배포 전에는 `npm ci`로 잠금 파일 그대로 설치하고 `npm run build:vendor`로 브라우저 번들을 다시 만든 뒤 `npm test`를 실행한다. 생성된 `scripts/vendor/manseryeok.browser.js`에는 번들 라이선스 고지가 포함된다.

## PWA 격리

- 제품 ID: `legend-manse`
- 내보내기 스키마: `schemaVersion: 2`
- 해석 규칙: `ruleVersion: "legend-1"`
- 저장 접두사: `legend-saju:`
- 캐시 접두사: `legend-manse-`

서비스워커의 프리캐시 목록은 첫 오프라인 진입에 필요한 HTML, CSS, JavaScript, 계산 엔진, 이미지, 아이콘을 포함한다. 다른 배포가 소유한 캐시는 삭제하지 않는다.

## CSP 한계

현재 앱은 큰 단일 `index.html` 안에 인라인 JavaScript와 인라인 스타일이 남아 있어 `script-src 'self'`만 허용하는 엄격한 Content Security Policy를 바로 적용할 수 없다. GitHub Pages에서도 응답 헤더 기반 nonce를 동적으로 발급할 수 없다.

향후 엄격한 CSP를 적용하려면 인라인 스크립트와 스타일을 외부 파일로 분리하고, 이벤트 속성을 제거한 뒤 `script-src 'self'`, `object-src 'none'`, `base-uri 'self'` 중심의 정책으로 전환해야 한다. 현재 릴리스는 XSS 회귀 테스트와 안전한 DOM 경로로 이 한계를 보완하지만 CSP 자체를 대체하지는 않는다.

## 검증 결과

- `npm test`: 통과
- 코어: 48개 통과, 실패·제외 0개
- 실제 브라우저 간지 fixture: 2개 기본 실행 통과
- UI: `360 / 390 / 412 / 768 / 1220px` 통과
- 200% 확대: 통과
- 페이지 예외: 0
- 콘솔 오류: 0
- 실패한 요청: 0
- 가로 넘침: 0
- 1~8운 메타데이터 회귀: 통과
- Android 전용 검사: 저장소에 `android/` 트리가 없어 명시적으로 제외

## 남은 고려사항

- 저장 데이터를 암호화하지 않으므로 기기 자체 잠금과 브라우저 프로필 보안이 중요하다.
- 엄격한 CSP는 인라인 코드 분리 작업 이후 적용해야 한다.
- 운세·공명 해석은 의학적·법적·재정적 판단을 대신하지 않으며 결과를 확정적으로 표현하지 않는다.
