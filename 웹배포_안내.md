# 취명선 전설의 만세력 웹 배포 안내

## 배포 주소

- 별도 저장소: `https://github.com/jansang18/legend-manse`
- GitHub Pages: `https://jansang18.github.io/legend-manse/`
- Pages 소스: `main` 브랜치의 저장소 루트

이 배포는 기존 `jansang18/sineum-manse` 저장소와 `https://jansang18.github.io/sineum-manse/`를 수정하지 않는다. 저장 키와 PWA 캐시도 각각 `legend-saju:`와 `legend-manse-`로 분리되어 원본 앱의 사용자 데이터와 오프라인 캐시를 건드리지 않는다.

## 계산 기준

### 공개 화면 입력 범위

- 입력 화면: `1026~2099년`
- 양력 `1908-04-01~2099년`: `manseryeok@2.0.0`의 KASI 기반 절기와 기록된 `Asia/Seoul` 민간시로 정밀 계산
- 양력 `1800~1908-03-31`: KASI 절기 데이터와 UTC+9 기준을 사용하되 역사 시각표 부재로 근사 계산 표시
- 음력 `1800~2099년`: 검증된 음양력 변환을 사용하며, 변환된 양력 날짜의 위 시간 범위 기준을 동일하게 적용
- 양력 `1026~1799년`: 기존 역사 범위 근사 계산으로 표시
- 음력 `1026~1799년`: 오변환을 막기 위해 거부하며 양력 입력을 안내

계산 엔진 API 자체는 양력 `1800~2300년`, 음력 `1800~2100년`을 지원하지만 현재 공개 입력 화면은 `2099년`까지 허용한다.

음력을 선택하면 `평달/윤달` 제어가 표시된다. 이 선택은 계산 입력, 저장 명반, JSON 내보내기에 함께 기록된다. 해당 연도에 존재하지 않는 윤달이나 월·일 범위를 입력하면 평달로 바꾸어 계산하지 않고 오류로 거부한다.

### 시간대와 일자 경계

- 시간대: 1908-04-01 이후 IANA `Asia/Seoul`의 역사적 민간시. 패키지가 지원하는 표준시 변경과 서머타임을 연주·월주·대운의 절입 순간에 반영
- 일주·시주: 출생지 경도·균시차를 적용하지 않고 사용자가 입력한 민간 벽시계 시각을 유지
- 태양시 보정: 기준 경도 `135°`, 균시차 보정 비활성. 출생 경도·진태양시 보정은 기본 적용하지 않음
- 연주·월주: 위 민간시의 절입 시각 기준
- 기본 일자 경계: 자정 `00:00`
- 기본 API 값: `dayBoundary: "midnight"`

출생 시각을 모르는 날짜의 00:00과 23:59 계산에서 연주 또는 월주가 달라지면 정오값으로 확정하지 않고 출생 시각 입력을 요구한다. 절입이 없는 일반 날짜는 기존처럼 시주만 `시간 모름`으로 두고 계산한다.

공개 입력 화면의 `고급 계산 설정`에서 아래 세 규칙을 선택할 수 있다. 선택은 브라우저에 저장되며 다음 방문에도 유지되고, 초기 기본값은 `midnight`다. `LegendGanji.calculate()` 또는 `calcSaju()`에서도 같은 값을 전달할 수 있다.

```js
const result = LegendGanji.calculate({
  year: 2024,
  month: 3,
  day: 10,
  hour: 23,
  minute: 30,
  calendar: 'solar',
  gender: 'M',
  unknown: false,
  dayBoundary: 'jasi'
});
```

- `midnight`: `00:00`에 일주 변경
- `jasi`: `23:00`부터 다음 날 일주 사용
- `splitJasi`: `23:00~23:59`의 일주는 당일로 두고 시주 계산에 다음 날 일간을 반영하는 야자시 분리 방식

## 재현 가능한 빌드

Node.js 18 이상과 Google Chrome이 필요하다.

```powershell
npm ci
npm run build:vendor
npm test
```

- `npm ci`: `package-lock.json`의 고정 버전 그대로 설치
- `npm run build:vendor`: `manseryeok@2.0.0`을 브라우저 번들로 재생성
- `npm test`: 코어·보안·PWA·UI 전체 릴리스 게이트 실행

UI 게이트는 로컬 HTTP 서버에서 `360`, `390`, `412`, `768`, `1220px`, 200% 확대, 가로 넘침, 페이지 예외, 콘솔 오류, 요청 실패를 검사한다.

기본 테스트에는 실제 브라우저에서 실행하는 계산·설정·음력 궁합·저장소·근거 모달 통합 회귀 6개와 전체 UI 회귀가 포함된다. UI 러너는 Windows, macOS, Linux의 Chrome·Chromium·Edge 일반 설치 위치를 자동 탐색한다. 다른 위치에 설치했다면 두 러너 모두 `CHROME_PATH`에 실행 파일의 절대 경로를 지정할 수 있다. 테스트가 브라우저를 자동 다운로드하지는 않는다.

## 백업 호환성

- 현재 형식은 `product: "legend-manse"`, `schemaVersion: 2`, `ruleVersion: "legend-1"`을 사용한다.
- 과거 실제 배포의 `신의 음성 만세력`, `취명선 만세력`, `취명선 전설의 만세력` v1 객체 백업 및 레코드 배열 백업을 가져올 수 있다.
- 구형 백업도 날짜·시간·성별 등 허용 필드를 엄격히 검사하고 파생 사주 값과 기존 ID를 신뢰하지 않는다.
- 정상 레코드는 현재 계산기로 다시 계산하고 새 UUID를 발급해 `legend-saju:record:`에만 기록한다.
- 알 수 없는 제품명·버전, 비배열 records, 잘못된 윤달·필드 타입은 거부하거나 해당 레코드를 건너뛴다.

## GitHub Pages 배포

별도 저장소가 준비된 뒤 현재 브랜치를 `main`으로 올린다.

```powershell
git remote add legend https://github.com/jansang18/legend-manse.git
git push -u legend HEAD:main
gh api --method POST repos/jansang18/legend-manse/pages -f source[branch]=main -f source[path]=/
```

Pages가 이미 활성화된 경우 마지막 명령은 생략하거나 GitHub 저장소의 `Settings > Pages`에서 `Deploy from a branch`, `main`, `/ (root)`를 선택한다.

## 배포 확인

1. `https://jansang18.github.io/legend-manse/`에서 제목이 `취명선 전설의 만세력`인지 확인한다.
2. 새로고침 후 개발자 도구의 Console과 Network에 오류가 없는지 확인한다.
3. 홈 화면 추가 후 독립 실행과 오프라인 재진입을 확인한다.
4. 명반 저장·내보내기·가져오기·공유를 확인한다.
5. 기존 `https://jansang18.github.io/sineum-manse/`가 그대로 열리고 기존 명반이 유지되는지 확인한다.

서비스워커 변경을 배포할 때는 `sw.js`의 `VERSION`을 올려 새 `legend-manse-` 캐시가 설치되도록 한다.
